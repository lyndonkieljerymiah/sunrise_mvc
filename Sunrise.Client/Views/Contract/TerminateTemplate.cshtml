@using Sunrise.Client.Infrastructure.Extension
@model Sunrise.Client.Domains.ViewModels.ContractTerminateViewModel

<form class="form-horizontal" model-state-attribute>
    <div class="modal-header">
        <h5 class="modal-title">Contract Termination Approval</h5>
    </div>
    <div class="modal-body">
        @Html.NgHiddenFor(m => m.ContractId, "$ctrl.model")
        <div class="form-group">
            <label class="col-md-3">Contract No: </label>
            <div class="col-md-9">
                @Html.NgInputFor(m => m.ContractCode, "$ctrl.model", "text", new string[] { "disabled" })
            </div>
        </div>
        <div class="form-group">
            <label class="col-md-3">Reason:</label>
            <div class="col-md-9">
                @Html.NgTextAreaFor(m => m.Description, "$ctrl.model")
                @Html.NgValidateFor(m => m.Description, "$ctrl.model")
            </div>
        </div>
        <div class="form-group">
            <label class="col-md-3">Reference No:</label>
            <div class="col-md-9">
                @Html.NgInputFor(m => m.ReferenceNo, "$ctrl.model", "text", new string[] { "placeholder='Enter Reference No'" })
                @Html.NgValidateFor(m => m.ReferenceNo, "$ctrl.model")
            </div>
        </div>
        <div class="form-group">
            <label class="col-md-3">Password:</label>
            <div class="col-md-9">
                @Html.NgInputFor(m => m.PassCode, "$ctrl.model", "password", new string[] { "placeholder='Verify Password'" } )
                @Html.NgValidateFor(m => m.PassCode, "$ctrl.model")
            </div>
        </div>
        <div class="modal-footer">
            <button class="btn btn-success btn-sm" ng-click="$ctrl.save()">Ok</button>
            <button class="btn btn-danger btn-sm" ng-click="$ctrl.cancel()">Cancel</button>
        </div>
    </div>
</form>
